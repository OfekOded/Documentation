============================================================
 HANDOFF PROMPT — Defense-Detection Project Report notebook
============================================================
(This file is for YOU. The AI does not need it — it reads AI_NOTEBOOK_GUIDE.md.)


HOW TO USE
----------
1. Open a new AI chat and attach these THREE files:
     a. AI_NOTEBOOK_GUIDE.md                        (the reference the AI must read)
     b. build_notebook.py                           (the notebook is GENERATED from this —
                                                      the AI edits this, NOT the .ipynb)
     c. Defense_Detection_Project_Report.ipynb      (the current report; optional but helpful)
2. Paste the prompt below.
3. Replace the <<< ... >>> line with your new progress (see "WHAT TO GIVE THE AI").


================= PASTE FROM HERE =================
You are extending my final-year project report notebook.

FIRST: read AI_NOTEBOOK_GUIDE.md in full — it explains the project, the two code
repositories, and (critically) that the notebook is GENERATED by build_notebook.py.
NEVER hand-edit the .ipynb: edit build_notebook.py, then run `python build_notebook.py`.

Here is the new progress I made in the project (dated notes / git commit messages /
new or changed file paths):
<<< PASTE YOUR NEW PROGRESS HERE — include dates, and point to the exact files/commits >>>

Please:
1. Ask me anything you need before writing.
2. Add the new work to the notebook IN CHRONOLOGICAL ORDER (insert where it belongs by
   date, not just at the end); renumber later steps if you insert in the middle (guide section 5).
3. Follow the conventions in the guide: NS-3 files as clickable links via ref(), ML files
   as paths via refml(), mark facts [VERIFIED] vs [HYPOTHESIS], prefer dated logs over
   recollection, keep it English.
4. Update the Table of Contents and the File Index to match.
5. Rebuild (`python build_notebook.py`) and run the validation snippet in guide section 6.
   Report the results.
================== TO HERE ==================


WHAT TO GIVE THE AI  (put this into the <<< ... >>> line above)
--------------------------------------------------------------
- DATES for each change (the report is chronological — this is how the AI places the work).
- The GIT COMMIT MESSAGES (or a `git log` excerpt) covering the new work.
- The FILE PATHS you added or changed, so the AI can cite real sources:
      * NS-3 files  -> it links them (repo is public)
      * ML files    -> it lists them by path (repo is private)
- Any RESULT FILES the new runs produced, so the AI lists them under "Outputs".
- A one-line note on whether each claim is measured/VERIFIED or a HYPOTHESIS.

Tip: the more precise the dates and commit messages, the more accurately the AI slots the
work into the timeline and cites real files — exactly how this report was built.
